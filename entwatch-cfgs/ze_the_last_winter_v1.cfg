"items"
{

	    "2"
  	{
        "name"          "Detonator"      // The "full" name of the item. (Used in CHAT)
        "short"         "Detonator"      // The "short" name of the item. (Used on the HUD)
        "color"         "808080"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "290269"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }
		"3"
  	{
        "name"          "Dynamite"      // The "full" name of the item. (Used in CHAT)
        "short"         "Dynamite"      // The "short" name of the item. (Used on the HUD)
        "color"         "ff4d00"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "462110"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }
		"4"
  	{
        "name"          "Dynamite"      // The "full" name of the item. (Used in CHAT)
        "short"         "Dynamite"      // The "short" name of the item. (Used on the HUD)
        "color"         "ff4d00"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "545003"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }
		"5"
  	{
        "name"          "Dynamite"      // The "full" name of the item. (Used in CHAT)
        "short"         "Dynamite"      // The "short" name of the item. (Used on the HUD)
        "color"         "ff4d00"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "480963"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }
		"6"
  	{
        "name"          "Dynamite"      // The "full" name of the item. (Used in CHAT)
        "short"         "Dynamite"      // The "short" name of the item. (Used on the HUD)
        "color"         "ff4d00"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "480946"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }
		"7"
  	{
        "name"          "Dynamite"      // The "full" name of the item. (Used in CHAT)
        "short"         "Dynamite"      // The "short" name of the item. (Used on the HUD)
        "color"         "ff4d00"      // The HEX color code of the item.
        "filter"        ""      // The targetname maps use for filtering. (Leave empty if none)
        "weaponid"      "480925"     // The HammerID of the weapon.
        "buttonid"      ""     // The HammerID of the button.
        "triggerid"     ""     // The HammerID of the trigger.
        "display"       "7"     // The Bitflag for displays.
                                // 1 = CHAT.
                                // 2 = USE.
                                // 3 = CHAT & USE.
                                // 4 = HUD.
                                // 5 = CHAT & HUD.
                                // 6 = USE & HUD.
                                // 7 = ALL.
        "slot"          "2"     // The weapon slot.
                                // 0 = None
                                // 1 = Primary
                                // 2 = Secondary
                                // 3 = Knife
                                // 4 = Grenades
        "mode"          "5"     // The mode of the item.
                                // 1 = Cooldown.
                                // 2 = Limited uses.
                                // 3 = Limited uses with cooldown.
                                // 4 = Cooldown after multiple uses.
        "maxuses"       "0"     // The maximum amount of uses.
        "cooldown"      "0"     // The cooldown between uses.
    }