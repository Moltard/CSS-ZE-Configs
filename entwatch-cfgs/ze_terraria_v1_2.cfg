"items"
{
	"0"
	{
		"name"          "Solar Eruption"      // The "full" name of the item. (Used in CHAT)
		"short"         "Solar Eruption"      // The "short" name of the item. (Used on the HUD)
		"color"         "eb4034"      // The HEX color code of the item.
		"filter"        "filter_fire"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "314680"     // The HammerID of the weapon.
		"buttonid"      "314609"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "1"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "60"     // The cooldown between uses.
	}
	"1"
	{
		"name"          "Terrablade"      // The "full" name of the item. (Used in CHAT)
		"short"         "Terrablade"      // The "short" name of the item. (Used on the HUD)
		"color"         "eb4034"      // The HEX color code of the item.
		"filter"        "filter_Terrablade"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "175239"     // The HammerID of the weapon.
		"buttonid"      "175232"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "1"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "10"     // The cooldown between uses.
	}
	"2"
	{
		"name"          "Guides Book"      // The "full" name of the item. (Used in CHAT)
		"short"         "Guides Book"      // The "short" name of the item. (Used on the HUD)
		"color"         "ad6a2b"      // The HEX color code of the item.
		"filter"        "Item_Ammo_Pistol"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "314363"     // The HammerID of the weapon.
		"buttonid"      "314360"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "1"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "60"     // The cooldown between uses.
	}
	"3"
	{
		"name"          "Portal Gun"      // The "full" name of the item. (Used in CHAT)
		"short"         "Portal Gun"      // The "short" name of the item. (Used on the HUD)
		"color"         "21bfc4"      // The HEX color code of the item.
		"filter"        "filter_portal_gun"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "8466"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"4"
	{
		"name"          "Bridge Part #1"      // The "full" name of the item. (Used in CHAT)
		"short"         "Bridge Part #1"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_bridge_part_1"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "129395"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"5"
	{
		"name"          "Bridge Part #2"      // The "full" name of the item. (Used in CHAT)
		"short"         "Bridge Part #2"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_bridge_part_2"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "129533"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"6"
	{
		"name"          "Bridge Part #3"      // The "full" name of the item. (Used in CHAT)
		"short"         "Bridge Part #3"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_bridge_part_3"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "129564"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"7"
	{
		"name"          "Bridge Part #4"      // The "full" name of the item. (Used in CHAT)
		"short"         "Bridge Part #4"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_bridge_part_4"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "149004"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"8"
	{
		"name"          "Bridge Part #5"      // The "full" name of the item. (Used in CHAT)
		"short"         "Bridge Part #5"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_bridge_part_5"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "149033"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"9"
	{
		"name"          "Temple Key"      // The "full" name of the item. (Used in CHAT)
		"short"         "Temple Key"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_temple_key"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "260024"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"10"
	{
		"name"          "Crimson Axe"      // The "full" name of the item. (Used in CHAT)
		"short"         "Crimson Axe"      // The "short" name of the item. (Used on the HUD)
		"color"         "66400a"      // The HEX color code of the item.
		"filter"        "filter_crimson_axe"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "128321"     // The HammerID of the weapon.
		"buttonid"      "0"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "0"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "0"     // The cooldown between uses.
	}
	"11"
	{
		"name"          "Heart Crystal"      // The "full" name of the item. (Used in CHAT)
		"short"         "Heart Crystal"      // The "short" name of the item. (Used on the HUD)
		"color"         "c43b66"      // The HEX color code of the item.
		"filter"        "filter_heart_crystal"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "18338"     // The HammerID of the weapon.
		"buttonid"      "18261"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "1"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "120"     // The cooldown between uses.
	}
	"12"
	{
		"name"          "Ice Crown"      // The "full" name of the item. (Used in CHAT)
		"short"         "Ice Crown"      // The "short" name of the item. (Used on the HUD)
		"color"         "342ca3"      // The HEX color code of the item.
		"filter"        "Item_freeze_filter"      // The targetname maps use for filtering. (Leave empty if none)
		"weaponid"      "323604"     // The HammerID of the weapon.
		"buttonid"      "323606"     // The HammerID of the button.
		"triggerid"     "0"     // The HammerID of the trigger.
		"display"       "7"     // The Bitflag for displays.
		                        // 1 = CHAT.
		                        // 2 = USE.
		                        // 3 = CHAT & USE.
		                        // 4 = HUD.
		                        // 5 = CHAT & HUD.
		                        // 6 = USE & HUD.
		                        // 7 = ALL.
		"slot"          "2"     // The weapon slot.
		                        // 0 = None
		                        // 1 = Primary
		                        // 2 = Secondary
		                        // 3 = Knife
		                        // 4 = Grenades
		"mode"          "1"     // The mode of the item.
		                        // 1 = Cooldown.
		                        // 2 = Limited uses.
		                        // 3 = Limited uses with cooldown.
		                        // 4 = Cooldown after multiple uses.
		"maxuses"       "0"     // The maximum amount of uses.
		"cooldown"      "60"     // The cooldown between uses.
	}
}	